name: "build"
description: "built static"
runs:
  using: "composite"
  steps:
    - uses: actions/setup-node@v1
      with:
        node-version: '14.x'
    - run: |
        npm install -g npm@7
        npm ci
        npm run build
      shell: bash
